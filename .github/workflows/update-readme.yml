name: Update README

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: sandip2224/sandip2224@master
        env:
          GITHUB_TOKEN: ${{ MY_SECRET.GITHUB_TOKEN }}
